CREATE TABLE IF NOT EXISTS student (
                                       rollNo INT PRIMARY KEY,
                                       name VARCHAR(255),
                                       marks INT
);
